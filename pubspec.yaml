name: photobooth_new
description: A photobooth app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.10.5+9
  image_picker: ^1.0.7
  path_provider: ^2.1.2
  qr_flutter: ^4.1.0
  gal: any # INI PENGGANTI IMAGE_GALLERY_SAVER
  permission_handler: ^11.3.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/frames/